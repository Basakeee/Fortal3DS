# Fortal3DS

Web-based procedural 3D model generator (Three.js) for producing game-ready assets — no Unity Editor or license needed to use it.

Scope: a **parameter/preset driven** generator (choose a theme preset, tweak numeric parameters like dimensions/proportions). It does not accept free-text prompts and does not call any AI text-to-3D service — that path was ruled out because AI-generated UVs are unreliable for production use.

## Current technique: voxel + vertex color

Each car preset is built as a grid of unit cubes (`voxelKit.js`), merged into a single mesh with **per-vertex color** instead of a textured UV layout. This sidesteps procedural UV-unwrapping entirely (a hard, unsolved problem for arbitrary generated shapes) at the cost of needing a vertex-color-aware shader on the Unity side (e.g. URP/Lit does **not** read vertex color by default — needs a Shader Graph with the Vertex Color node, or a custom shader).

Colors are drawn from a shared 64-swatch palette (`palette.js`) so future themes stay visually consistent instead of each one inventing its own colors.

## Presets

- `carGenerator.js` — generic parameterized 90s sedan (boxy body, upright greenhouse).
- `ae86Generator.js` — Toyota Sprinter Trueno AE86: hatchback proportions, "panda" two-tone paint (position-dependent, not a single flat body color), pop-up headlights, signature black rear garnish with red lens segments. A specific real car needed its own layout rather than a sedan-preset variant, since panda paint and pop-up lights aren't expressible as sedan parameters.
- `humanGenerator.js` — voxel human character (head/neck/torso/arms/hip/legs/feet), scaled to a target real-world height rather than a fixed voxel size. Presets (`HUMAN_PRESETS.male/female/child`) mirror the player-size options in the Fortal Main Arena sim (170/158/120 cm) — built to replace that sim's current placeholder (an armless torso-and-leg box). Preview at `human.html` / `humanMain.js` with a preset switcher and shirt-color picker. Also exports `generateFarmerRig`/`generateFarmerMesh` for Memory Farm's parade farmer — same body, but the shirt is a SEPARATE mesh/node (`Farmer_Body` + `Farmer_Shirt`, both under a `Farmer` group) with a plain, non-vertex-colored material, so its color can be changed at runtime in Unity (one imported model instead of baking a separate export per `farmerTypes` color — a straw hat rides along on `Farmer_Body` since it isn't recolorable). Two nodes surviving the Blender round-trip is the same technique `dragonGhastGenerator.js`'s `TentaclePivot_0..8` nodes already proved out, not a new unverified export path.
- `dragonGhastGenerator.js` — boss creature for the Boss Slayer arena game: same silhouette as Minecraft's Ghast (cubic floating body + a grid of dangling tentacles), re-skinned as a dragon — scale-pattern body, lighter belly, slit reptilian eyes, horns, and a protruding toothed snout instead of Ghast's flat white face. Preview at `dragon.html` / `dragonMain.js`, which can export it two ways — see "Animated export" below.
- `demonBossGenerator.js` — second Boss Slayer creature: a cute cube blob, not a biped — one dominant cube body carries the face directly (no separate head/neck/torso, and no limbs at all — sits flush on the ground), with only horns and a tail as appendages. Red skin, pink belly patch, big white-over-black eyes, a single center fang, curled black horn tips.
- `giantFishGenerator.js` — third Boss Slayer creature: same one-dominant-block philosophy as the demon boss, elongated instead of cube-shaped. No limbs, but fins are load-bearing this time (a fish needs at least a tail fin to read as a fish) — a tapered dorsal fin, thin flat pectoral fins, and a wide tail fin taller than the body itself. Deep blue skin, sky-blue belly, gunmetal fins, comb-pattern teeth across the whole front width.
- `jellyfishBossGenerator.js` — fourth Boss Slayer creature (บาส 2026-09-03): a voxel copy of the reference boss from the Boss Slayer web arena-sim prototype (aowallop.github.io/Ao-Presentation/main-arena-sim). Reuses `dragonGhastGenerator.js`'s Ghast silhouette (cubic body + a grid of dangling tentacles) directly rather than inventing a new body plan — a jellyfish's real anatomy already is "dome + tentacles." Purple checkerboard skin, two square red glow eyes, dark banded tentacles, no horns/snout/mouth (the reference has none). Also has its own animated-tentacle-sway rig (`generateJellyfishBossRig`/`assembleJellyfishBossAnimatedRig`, preview at `jellyfish.html`/`jellyfishMain.js`) — same technique and export options as `dragonGhastGenerator.js`'s, see "Animated export" below.
- `crystalGolemBossGenerator.js` — fifth Boss Slayer creature: one dominant cube body like the demon boss, but taller/blockier (reads heavy, not cute-round), checkerboarded rock-facet skin instead of a flat color, and a jagged crystal-shard crown (varying-height spikes) instead of horns. Glowing cyan eye slits, no tail.
- `voidWraithBossGenerator.js` — sixth Boss Slayer creature: same one-dominant-block philosophy, but the identity feature sits at the base instead of a tail/fin/horn — the bottom rows flare wider than the body (same technique the giant fish's tail fin uses to extend past its own bounds), reading as a robe/cloak hem. Dark violet body, darker hem, glowing purple eye slit. No horns, no mouth.
- `mechaSentinelBossGenerator.js` — seventh Boss Slayer creature: hard-edged instead of organic — horizontally-banded plating (checkerboard technique reused from the dragon's scale pattern, just on the Y axis) reading as armor seams, one glowing horizontal visor band instead of two eyes, and a single antenna spike (tip glowing the same color as the visor) instead of paired horns.
- `stormSerpentBossGenerator.js` — eighth Boss Slayer creature: reuses `giantFishGenerator.js`'s elongated one-block-plus-fins body plan, reskinned with a checkerboard scale pattern (like the dragon's) instead of a flat body color, a jagged head crest (varying-height spikes, same technique the crystal golem's crown uses) instead of pectoral fins, and no teeth/mouth. Teal-green scales, bright teal fins, glowing red eyes.
- `dragonWingsBossGenerator.js` — ninth Boss Slayer creature: a SECOND, brand-new dragon (not an edit of `dragonGhastGenerator.js`'s existing dragon, which is already placed in the Unity scene as "Dragon Boss 1" and can't be touched by this pipeline). บาส's ask was "take the existing dragon, remove the whiskers, give it legs and wings instead" — since that specific model can't be edited, this is a fresh dragon built to that spec: warm red/ember checkerboard scales (not the Ghast dragon's green), four short leg stubs (a stance, not articulated limbs — same economy of detail as the demon boss's tail), wing panels (reusing the giant fish's flat pectoral-fin technique, sized/positioned to read as wings), and deliberately no whisker/barbel appendages anywhere on the head.
- `airplaneGenerator.js` — generic blocky prop fighter (fuselage, wings, tailplane/fin, static 2-blade propeller). No landing gear — at this voxel resolution thin gear legs read as noise, not detail.
- `tankGenerator.js` — generic battle tank: hull, side tracks with evenly spaced road wheels, turret set back from the hull's front so the barrel can overhang the nose.
- `treeGenerator.js` — oak tree: banded-bark trunk, canopy is a blocky sphere (distance test, not a box) so it reads as a tree silhouette from any angle.
- `tableGenerator.js` — four legs and a slab, no joinery detail. The deliberately plainest preset in the set.
- `gemGenerator.js` — not a boss, a pickup: the item players collect to attack the boss (see `BossSlayerGameManager.cs`'s `spotDefs`, which spawns one of these as a flat UI circle today). A stepped bipyramid (wide middle girdle, flat top facet, tapered bottom point), two-tone shine, one sparkle voxel. `GEM_PRESETS.attack/heal/stun/ultimate` are colored to match that file's own `spotDefs` `Color` values exactly, not a separate palette — the facet's lighter tone is derived from whatever `gemColor` is passed in (see `lighten()`), so a custom color still gets a correct two-tone shine instead of only the four presets working right.
- `coinGenerator.js` — bounty-reward pickup for a separate, not-yet-built game (Bounty Hunter), same "collectible, not a boss" role as `gemGenerator.js`. A flat voxel disc (circular via a distance test, not a cube-faced block — same technique `treeGenerator.js`'s canopy uses), with a darker milled-edge rim, a lighter raised-looking center emblem, and a two-tone checkerboard shimmer across the rest of the face. `COIN_PRESETS.gold/silver/bronze` are the typical bounty-reward tiers; rim/emblem tones derive from whatever `coinColor` is passed in (see `shade()`), so a custom color still shades correctly. Deliberately thin on Y, wide on X/Z — spinning it around Y (its only export is a static mesh; do the spin in Unity, see Status below) sweeps face → edge → face, the way a flipped coin actually reads, unlike gemGenerator/most other presets where Y is the tall axis.
- `glassTileGenerator.js` — one floor cell for Mirror Path (`MirrorPathGameManager.cs`), which currently renders each cell as a flat-tinted `RawImage` in its `hidden`/`safe`/`break`/`broken`/`wrong` colors (see `PaintPathCells`/`AddBroken`). Two nodes, same split as `humanGenerator.js`'s Farmer_Body/Farmer_Shirt: `GlassTile_Frame` (vertex-colored metal bezel, fixed) and `GlassTile_Pane` (separate, uniformly-colored mesh — leave its imported Unity material on default URP/Lit instead of reassigning `Fortal/VertexColorLit`, and recolor it live via script to match the 5 cell states, no per-state export needed). The pane's "glass" read comes entirely from voxel-height faceting (a shallow 2-tier dome), not vertex-color shine — a plain-material node's vertex color never survives to Unity anyway, so only real mesh shape carries through that path. Also exports an animated break variant (`generateGlassTileBreakRig`/`assembleGlassTileBreakRig`, preview at `glassTile.html`/`glassTileMain.js`): the pane splits into a 3x3 grid of `GlassTile_Shard_0..8` nodes that fall/tumble outward from a shared `shardBreakTransformAt()` formula, baked as a **one-shot** `GlassBreak` `AnimationClip` (not a loop, unlike `dragonGhastGenerator.js`'s `TentacleSway`) — see "Animated export" below for the Unity-side playback difference that implies.
- `pigGenerator.js` / `dogGenerator.js` / `cowGenerator.js` / `catGenerator.js` / `chickenGenerator.js` — the 5 farm animals `MemoryFarmGameManager.cs`'s `animalTypes` currently only distinguishes by tinting one flat placeholder sprite (see its own `NamedColor[]`); each generator's default color matches that file's `Color(...)` value exactly, same "reads as the same item" reasoning `gemGenerator.js`'s `GEM_PRESETS` gives for Boss Slayer's pickups. Modeled directly on Minecraft's own mob silhouettes (per บาส), simplified to one dominant body block with NO separate legs — same "single cube, no limbs" philosophy `demonBossGenerator.js`/`giantFishGenerator.js` already use, not the earlier 4-leg-column design this went through first. Species differ by Minecraft's own identifying features rather than an invented distinction: pig/cow get a protruding square snout nub, cow adds hand-placed black coat patches (a plain-colored blob doesn't read as "cow" the way it still reads as "pig"/"cat") plus small horns, dog/cat both get upright tapered ears (a wolf's, not floppy) with cat's the smaller/closer-set of the two and the longest tail of the 4. `chickenGenerator.js` is the only biped-turned-boxy exception — comb/beak/wattle/wings carry its identity since a box alone doesn't read as "chicken" the way it reads as every 4-legged mammal here.

## Status

Confirmed working end-to-end (2026-08-31): `.glb` (this repo) → Blender (import glTF, export `.fbx`) → Unity import → custom URP shader `Fortal/VertexColorLit` (reads the mesh's vertex color directly) → correct color in-scene.

The one non-obvious step: Blender's material for an imported glTF mesh is a node graph (Attribute node → Base Color), and FBX's material format can't represent a node graph — only a flat color — so the material Unity extracts from the `.fbx` comes back plain grey even though the vertex colors are intact on the mesh itself. `Fortal/VertexColorLit` (see the `deepspace` Unity project's `Assets/Shaders/`) sidesteps this by reading the vertex color directly instead of depending on the imported material.

## Preset gallery

`index.html` / `main.js` is the main entry point: a sidebar lists every preset above, grouped by category. Clicking a preset's button generates it into the shared viewer; **Export .glb (current)** exports whichever preset is currently shown. Adding a future preset is "add one entry to `PRESET_GROUPS` in `main.js`" — the viewer, camera framing, and export logic are all generic over `mesh.geometry.boundingSphere` and don't know about any specific preset.

Each preset also has its own checkbox (บาส 2026-09-03) — check any number of them, independent of which one is currently previewed, and **Export selected (N)** generates + downloads each checked preset's `.glb` one after another (each its own temporary mesh, disposed right after its own export, so batch-exporting never leaves the viewer showing some unrelated last-one-exported mesh). Downloads are spaced ~400ms apart rather than fired all at once, since Chrome's own "this site wants to download multiple files" prompt kicks in after a handful of rapid-fire downloads — if it still prompts, allow multiple downloads for the page and click **Export selected** again. **ล้าง** clears every checkbox.

`human.html` / `dragon.html` / `jellyfish.html` stay as separate pages on top of that — they carry extras the gallery doesn't (human's shirt-color swatches, dragon's and jellyfish's animated tentacle-sway previews) rather than just being older copies of the same thing.

## Run locally

Needs a local server — opening `index.html` directly (`file://`) fails because browsers block ES module imports over `file://`.

```bash
npx serve .
```

Then open the printed `http://localhost:...` URL. Click **Export .glb**, then drag the downloaded file into a Unity project's `Assets/` folder and check scale, vertex colors, and normals survive the round-trip.

## Batch export without a browser

`exportAll.mjs` runs every preset's exporter in Node (no page/click needed) and writes `.glb` files straight to `exports/` (gitignored — treat it as a local build output, not something to commit).

```bash
npm install
npm run export
```

Node has no `FileReader` (which `GLTFExporter`'s binary path uses internally), so the script shims a minimal one backed by Node's global `Blob.arrayBuffer()` — see the top of `exportAll.mjs`.

## Animated export (multi-node rigs)

Three presets so far export a multi-node animated rig alongside their single-mesh static export: `dragon.html` (Dragon Ghast's tentacle sway), `jellyfish.html` (Jellyfish Boss's tentacle sway), and `glassTile.html` (the glass tile's break). All follow the same shape — a Group of named pivot nodes plus a baked `AnimationClip`, with one shared pure timing/transform function feeding the live preview, both export paths, and the headless batch export so they can never drift onto different motion:

- **Dragon Ghast — `dragon.html`:** **Export .glb (static)** is the single merged mesh every other preset also exports (the round-trip verified through Blender/FBX into Unity, see Status above). **Export .glb (animated tentacles)** — `assembleDragonGhastAnimatedRig()` in `dragonGhastGenerator.js` splits the 9 tentacles into their own named nodes (`TentaclePivot_0`..`8`) instead of merging them into the body, and ships a baked, **looping** `TentacleSway` `AnimationClip` (9 `QuaternionKeyframeTrack`s, 24fps, 6s, looped by snapping the last frame back to the first) driving their rotation.

- **Jellyfish Boss — `jellyfish.html`:** Exact same rig shape as Dragon Ghast above (`assembleJellyfishBossAnimatedRig()` in `jellyfishBossGenerator.js`, same `TentaclePivot_0`..`8` naming, same looping `TentacleSway` clip) — its 9 tentacles use their own, wider amplitude range (thinner/limper than the dragon's scaled tentacles, so they read as trailing/drifting rather than rigid), but the timing/loop mechanics are identical. **Not yet verified through the Blender/FBX round-trip or in Unity** — only the dragon's own rig has been (see the caveat below), and that caveat almost certainly applies here too since it's the same node/clip shape.

- **Glass Tile — `glassTile.html`:** **Export .glb (static)** is the intact `GlassTile_Frame` + `GlassTile_Pane` 2-node group (see the preset entry above). **Export .glb (animated break)** — `assembleGlassTileBreakRig()` in `glassTileGenerator.js` splits the pane into a 3x3 grid of `GlassTile_Shard_0..8` nodes and ships a baked, **one-shot** `GlassBreak` `AnimationClip` (position + rotation tracks per shard, falling/tumbling outward, sampled straight from t=0 to t=1 with **no** closing-frame snap back to the rest pose — unlike the dragon's loop). That difference matters on the Unity side: the dragon's fix (Legacy Animation Type + a `Legacy` `Animation` component that just plays and loops) isn't directly reusable here — a one-shot fall needs its wrap mode set to Once/ClampForever (Legacy) or an Animator state with Loop Time off, triggered on break rather than always-on. **Not yet verified through the actual Blender/FBX round-trip or in Unity** (only the dragon's tentacle rig has been, per the caveat below — the glass tile rig hasn't been import-tested at all yet).

**Confirmed working end-to-end through Unity for the dragon's tentacle rig (2026-08-31), with a caveat that likely applies to any multi-node clip exported from here, including the glass tile's:** a single multi-node `AnimationClip` does **not** survive Blender → FBX intact — Blender ties one Action to one object, so it splits into **N separate single-track clips** on import (e.g. `TentaclePivot_0|TentacleSway` .. `_8`), and Unity's Model Importer brings those in as **Generic**-type clips. An Animator Controller can't play N independent simultaneous clips on N different objects without an Avatar Mask per layer, which is a lot of setup for clips whose curves don't even overlap. The dragon's actual fix: switch the FBX's Rig ▸ Animation Type to **Legacy**, add a legacy `Animation` component to the boss root with all 9 clips in its list, and `Assets/Scripts/Deepspace/TentacleSwayPlayer.cs` (in the `deepspace` Unity project) plays all 9 simultaneously on separate layers in `OnEnable()` (not `Start()` — the boss is `SetActive(false)`d during setup and only shown later, and `Start()` only ever fires once per component lifetime, so it can lose the race against that hide/show sequence). A glass-tile equivalent player script would need to trigger those N clips **once** on break instead of always-on-loop — that script doesn't exist yet.

## Next steps

- Confirm the vertex-color round-trip actually renders correctly in Unity (needs a vertex-color shader — not guaranteed to "just work").
- Add CSG boolean geometry (e.g. `three-bvh-csg`) for hard-surface theme presets (ship parts, weapons) once a non-voxel style is needed.
- Add noise-displaced sphere geometry for organic theme presets (asteroids, rocks).
